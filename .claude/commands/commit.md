Generate a Conventional Commit message based on the current staged changes (run git diff --cached to inspect them).

Format: type(scope): short description

Allowed types: feat, fix, refactor, docs, test, chore, perf, style.

Propose the message and wait for my approval before running git commit.
